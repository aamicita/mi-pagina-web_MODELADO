options:
  logging: CLOUD_LOGGING_ONLY

steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
    - 'build'
    - '-t'
    - 'gcr.io/$PROJECT_ID/mi-pagina:$COMMIT_SHA'
    - '.'

- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
    - run
    - deploy
    - mi-pagina
    - --image
    - gcr.io/$PROJECT_ID/mi-pagina:$COMMIT_SHA
    - --region
    - us-central1
    - --platform
    - managed
    - --allow-unauthenticated

images:
- 'gcr.io/$PROJECT_ID/mi-pagina:$COMMIT_SHA'